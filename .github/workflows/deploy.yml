name: 'Deploy frontend'

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 'Baixar código'
        uses: actions/checkout@v4

      - name: 'Configurar Node.js'
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 'Instalar dependências'
        run: npm ci

      - name: 'Instalar NX globalmente'
        run: npx nx build frontend_nepa --configuration=production

      - name: 'Verificar arquivos de build'
        run: ls -la ./dist/frontend_nepa/browser/

      - name: 'Testar conexão básica'
        run: |
          echo "Host: ${{ secrets.SSH_HOST }}"
          echo "Port: ${{ secrets.SSH_PORT }}"
          echo "Username: ${{ secrets.SSH_USERNAME }}"
          echo "Password: ${{ secrets.SSH_PASSWORD }}"

      - name: 'Limpar diretório no servidor'
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }} # 🔑 TROCA AQUI
          script: |
            rm -rf /frontend/*
            mkdir -p /frontend

      - name: 'Copiar arquivos para servidor'
        uses: appleboy/scp-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }} # 🔑 TROCA AQUI
          port: ${{ secrets.SSH_PORT }}
          source: './dist/frontend_nepa/browser/*'
          target: '/frontend'
          overwrite: true
